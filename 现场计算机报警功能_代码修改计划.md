# 现场计算机报警功能 - 代码修改计划

## 一、需求分析

### 1.1 原始文档要求
根据 `现场计算机报警功能.docx`，4路继电器板功能分配如下：

| 继电器 | 功能 | 输出特性 | 当前状态 |
|--------|------|----------|----------|
| 继电器1 | 相机断电复位 | 常闭触点串入5V电源，触发时断开 | ✅ 已实现 |
| 继电器2 | 在线表面检测报警输出 | 持续输出，需人工点击界面复位 | ✅ 已实现 |
| 继电器3 | 端面检测报警输出 | **1秒脉冲后自动复位** | ❌ **未实现** |
| 继电器4 | 表面检测报警输出 | **1秒脉冲后自动复位** | ❌ **未实现** |

### 1.2 当前代码状态

**已实现的功能：**
- `serial.ts`: 继电器1（相机电源）、继电器2（报警输出）的开关控制
- `relayController.ts`: 相机电源控制的API端点
- `lightController.ts`: 继电器2与报警灯联动控制

**缺失的功能：**
- 继电器3（端面检测报警）的脉冲控制
- 继电器4（表面检测报警）的脉冲控制
- 脉冲持续时间管理（1秒）

---

## 二、技术方案

### 2.1 继电器控制协议

基于现有代码中的协议格式：

```
格式: A0 地址码 操作码 校验码
校验码 = A0 + 地址码 + 操作码
```

**各继电器地址码：**
- 继电器1: `0x01`
- 继电器2: `0x02`
- 继电器3: `0x03` (新增)
- 继电器4: `0x04` (新增)

**操作码：**
- `0x00`: 关闭继电器
- `0x01`: 打开继电器
- `0x05`: 查询状态

**计算出的完整命令：**
| 操作 | 继电器3命令 | 继电器4命令 |
|------|-------------|-------------|
| 打开 | `A0 03 01 A4` | `A0 04 01 A5` |
| 关闭 | `A0 03 00 A3` | `A0 04 00 A4` |

### 2.2 脉冲功能实现方案

**方案：Promise + setTimeout**
```typescript
// 触发1秒脉冲
async function triggerPulse(relayNumber: 3 | 4): Promise<void> {
  await turnOnRelay(relayNumber);
  await delay(1000);  // 等待1秒
  await turnOffRelay(relayNumber);
}
```

**优势：**
- 简单直观
- 易于测试
- 不需要维护定时器状态

**考虑因素：**
- 需要防止并发触发（如果正在脉冲中，新的触发请求如何处理？）
- 建议采用队列或状态锁机制

---

## 三、具体修改计划

### 阶段1：核心服务层扩展 (serial.ts)

**文件**: `/weixing-service/src/services/serial.ts`

**新增函数：**

```typescript
// 打开继电器3
export async function turnOnRelay3(): Promise<boolean>

// 关闭继电器3
export async function turnOffRelay3(): Promise<boolean>

// 打开继电器4
export async function turnOnRelay4(): Promise<boolean>

// 关闭继电器4
export async function turnOffRelay4(): Promise<boolean>

// 触发继电器3脉冲（1秒）
export async function triggerRelay3Pulse(durationMs: number = 1000): Promise<boolean>

// 触发继电器4脉冲（1秒）
export async function triggerRelay4Pulse(durationMs: number = 1000): Promise<boolean>
```

**脉冲状态管理：**
```typescript
// 防止并发触发的锁
const relayPulseLocks: Map<number, boolean> = new Map();

// 检查是否正在脉冲中
function isRelayPulsing(relayNumber: number): boolean

// 设置脉冲状态
function setRelayPulseLock(relayNumber: number, locked: boolean): void
```

---

### 阶段2：控制器层扩展 (relayController.ts)

**文件**: `/weixing-service/src/controllers/relayController.ts`

**新增端点处理函数：**

```typescript
// 触发继电器3脉冲（端面检测报警）
export async function triggerRelay3PulseEndpoint(req: Request, res: Response)

// 触发继电器4脉冲（表面检测报警）
export async function triggerRelay4PulseEndpoint(req: Request, res: Response)

// 手动控制继电器3开关
export async function turnOnRelay3Endpoint(req: Request, res: Response)
export async function turnOffRelay3Endpoint(req: Request, res: Response)

// 手动控制继电器4开关
export async function turnOnRelay4Endpoint(req: Request, res: Response)
export async function turnOffRelay4Endpoint(req: Request, res: Response)
```

---

### 阶段3：路由层扩展 (relayRoutes.ts)

**文件**: `/weixing-service/src/routes/relayRoutes.ts`

**新增路由：**

```typescript
// 继电器3控制（端面检测）
router.post("/relay-3/pulse", triggerRelay3PulseEndpoint);      // 触发1秒脉冲
router.post("/relay-3/on", turnOnRelay3Endpoint);               // 手动打开
router.post("/relay-3/off", turnOffRelay3Endpoint);             // 手动关闭

// 继电器4控制（表面检测）
router.post("/relay-4/pulse", triggerRelay4PulseEndpoint);      // 触发1秒脉冲
router.post("/relay-4/on", turnOnRelay4Endpoint);               // 手动打开
router.post("/relay-4/off", turnOffRelay4Endpoint);             // 手动关闭
```

---

### 阶段4：API测试文件更新

**文件**: `/weixing-service/relay.http`

**新增测试请求：**

```http
### 触发继电器3脉冲（端面检测报警 - 1秒）
POST http://localhost:3001/api/relay/relay-3/pulse
Content-Type: application/json

### 触发继电器4脉冲（表面检测报警 - 1秒）
POST http://localhost:3001/api/relay/relay-4/pulse
Content-Type: application/json
```

---

## 四、文件修改清单

| 序号 | 文件路径 | 修改类型 | 修改内容 |
|------|----------|----------|----------|
| 1 | `src/services/serial.ts` | 新增 | 继电器3、4的基础控制和脉冲函数 |
| 2 | `src/controllers/relayController.ts` | 新增 | 继电器3、4的API端点处理函数 |
| 3 | `src/routes/relayRoutes.ts` | 新增 | 继电器3、4的路由定义 |
| 4 | `relay.http` | 更新 | 新增测试请求示例 |
| 5 | `API_DOCUMENTATION.md` | 更新 | 补充新API文档 |

---

## 五、API设计

### 5.1 触发脉冲（主要功能）

**POST** `/api/relay/relay-3/pulse`

**POST** `/api/relay/relay-4/pulse`

**请求体（可选）：**
```json
{
  "duration": 1000  // 脉冲持续时间（毫秒），默认1000ms
}
```

**成功响应：**
```json
{
  "success": true,
  "message": "继电器3脉冲已触发",
  "relay": 3,
  "duration": 1000,
  "command": "A0 03 01 A4"
}
```

**失败响应（正在脉冲中）：**
```json
{
  "success": false,
  "message": "继电器3正在执行脉冲，请稍后再试",
  "relay": 3,
  "status": "pulsing"
}
```

### 5.2 手动控制

**POST** `/api/relay/relay-3/on`
**POST** `/api/relay/relay-3/off`
**POST** `/api/relay/relay-4/on`
**POST** `/api/relay/relay-4/off`

---

## 六、实施顺序

```
Step 1: serial.ts
    ├── 添加 turnOn/OffRelay3/4 基础函数
    ├── 添加 triggerRelay3/4Pulse 脉冲函数
    └── 添加并发控制锁机制

Step 2: relayController.ts
    ├── 导入新增函数
    ├── 实现 pulse endpoint 处理函数
    └── 实现 on/off endpoint 处理函数

Step 3: relayRoutes.ts
    └── 注册新路由

Step 4: 测试验证
    ├── 使用 relay.http 测试
    ├── 验证1秒脉冲时长
    └── 验证并发控制

Step 5: 文档更新
    └── 更新 API_DOCUMENTATION.md
```

---

## 七、注意事项

1. **并发控制**：脉冲执行期间应拒绝新的触发请求，或采用队列机制
2. **错误处理**：串口通信失败时需要适当重试或返回错误
3. **日志记录**：所有脉冲触发应记录日志，便于调试
4. **配置化**：脉冲持续时间应支持配置，默认1000ms
5. **测试验证**：需要实际硬件测试确认1秒脉冲时长准确

---

## 八、测试策略

### 8.1 单元测试
- 验证命令字节计算正确
- 验证脉冲时序
- 验证并发控制

### 8.2 集成测试
- 通过HTTP接口触发脉冲
- 验证硬件实际响应
- 验证1秒时长的准确性

### 8.3 边界测试
- 快速连续触发（测试并发锁）
- 串口断开时的错误处理
- 非标准持续时间（如0ms、负数）

---

*计划创建时间: 2026-03-13*
*基于文档: 现场计算机报警功能.docx*
